linters:
  enable:
    - errcheck
    - govet
    - staticcheck

issues:
  exclude-rules:
    # Test files can be more lenient about error checking
    - path: _test\.go
      linters:
        - errcheck
